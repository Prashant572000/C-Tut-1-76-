//  Operator - is a symbol used to perform operation. (Special symbols that used to perform actions or operations )
#include <stdio.h>
void main()
{
    // int a=9 , b=10;
    int a, b, x;
    // printf("a+b = %d\n", a + b);                 // Arithmetic operators
    // printf("a*b = %d\n", a * b);
    // printf("a/b = %d\n", a / b);
    // printf("a % b = %d\n", a%b);
    // printf("a equal to %d\n" , a==b);           // Relational operators - if equal return 1 else 0
    // printf("a and b = %d\n" , a&&b);               // Logical Operators , if a &b T then return True else False
    // printf("a or b = %d\n" , a||b);
    // printf("a is not equal b = %d\n" , !b);
    a = 2;
    b = 3;
    // printf("a & b = %d\n" , a&b);                   // Bitwise Operators(convert given no. to 0 1 form) works bit by bit
}
// Exclusive OR (^) it wants 1 val T & 1 val F then return True , else False
// sizeof() returns size of var